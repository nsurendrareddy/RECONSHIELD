{
  "name": "reconshield",
  "version": "1.0.0",
  "description": "ReconShield – Ethical Intelligence & Security Analyzer",
  "private": true,
  "scripts": {
    "client": "cd client && npm run dev",
    "client:install": "cd client && npm install",
    "client:build": "cd client && npm run build"
    "react-quill-new": "^2.0.0",
  }
}
